/* The one line you edit.
 *
 * Paste the receiver into a Deno Deploy playground (see ../README.md), then put its URL here.
 *
 * There is no key on this side and there never can be: this file is public, served to every
 * visitor who opens the page. The only secret in the system is the GitHub token, and it
 * lives in the receiver, which is the whole reason the receiver exists.
 *
 * Until this is a real URL the form stays disabled and says so, rather than accepting an
 * address it has nowhere to put.
 */
window.COLAI_WAITLIST = {
  endpoint: "https://frozen-capybara-6040.shahar-nadiv.deno.net",
};
